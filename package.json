{
  "name": "verity",
  "version": "0.1.0",
  "private": true,
  "description": "VERITY \u2014 proving board authorisation in zero knowledge on Midnight. Brainwave 2026 \u00b7 Midnight Track, Team Logarithm.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck --if-present",
    "smoke": "node scripts/midnight-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
